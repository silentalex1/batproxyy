[build]
  command = "npm run build"
  publish = "dist"

# Cloudflare Pages handles SPA via public/_redirects and headers via public/_headers
# This file retained for Netlify compatibility only

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
